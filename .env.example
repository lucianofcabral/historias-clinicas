# Base de Datos
DATABASE_URL=postgresql://user:password@localhost:5432/medical_records_db

# Usuario único - Generar con: uv run python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['argon2']).hash('tu_password'))"
ADMIN_PASSWORD_HASH=tu_hash_aqui

# App
APP_NAME=Historias Clínicas
DEBUG=True
ENVIRONMENT=development

# Backups
BACKUP_ENABLED=True
BACKUP_FREQUENCY_DAYS=7
BACKUP_PATH=./backups
